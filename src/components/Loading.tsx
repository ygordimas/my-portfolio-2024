import React from 'react';
import { Antenna } from 'iconoir-react';

const Loading = () => {
  return (
    <div className="flex justify-center items-center">
      <Antenna />
    </div>
  );
};

export default Loading;
